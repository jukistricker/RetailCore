<nav class="navbar navbar-expand-lg navbar-custom sticky-top">
    <div class="container">
        <!-- Logo -->
        <a class="navbar-brand fw-bold" href="/" style="color: var(--primary-blue);">
            <i class="bi bi-bag-check-fill"></i> RetailCore
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarContent">
            @await Component.InvokeAsync("CategoryMenu")

            <!-- Thanh tìm kiếm -->
            <form class="d-flex search-container mx-auto" action="/Search" method="GET" style="flex-grow: 1; max-width: 500px;">
                <input class="form-control search-input" type="search" placeholder="Search products..." name="q">
                <button class="search-btn" type="submit">
                    <i class="bi bi-search"></i>
                </button>
            </form>

            <div class="d-flex align-items-center ms-auto">
                <!-- Theme Toggle -->
                <button id="theme-toggle" class="btn nav-icon-link border-0 bg-transparent"
                        title="Toggle Light/Dark Mode">
                    <i class="bi bi-moon-fill" id="theme-icon"></i>
                </button>


                <!-- Cart -->
                <a href="/Cart" class="nav-icon-link">
                    <i class="bi bi-cart"></i>
                    <span class="badge rounded-pill bg-danger badge-count">3</span>
                </a>

                <!-- Account Dropdown -->
                <div class="dropdown mx-2">
                    <a class="nav-icon-link" href="#" role="button" data-bs-toggle="dropdown">
                        <i class="bi bi-person"></i>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end">
                        <li><a class="dropdown-item" href="/Customer/Profile">My Profile</a></li>
                        <li><a class="dropdown-item" href="/Orders">My Orders</a></li>
                        <li>
                            <hr class="dropdown-divider">
                        </li>
                        <li>
                            <form asp-controller="Auth" asp-action="Logout" method="post" id="logoutForm" class="d-inline">
                                @Html.AntiForgeryToken()
                                <button type="submit" class="dropdown-item text-danger border-0 bg-transparent">
                                    Logout
                                </button>
                            </form>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</nav>